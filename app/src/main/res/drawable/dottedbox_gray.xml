<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 虚线框，宽度为dashWith，空隙的宽度为dashGap -->
    <stroke
        android:dashGap="2dp"
        android:dashWidth="5dp"
        android:width="1dp"
        android:color="@color/deep_gray" />

</shape>